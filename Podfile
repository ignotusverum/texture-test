platform :ios, '9.0'

use_frameworks!

def shared_pods
    
    inhibit_all_warnings!
    # Texture
    pod 'Texture', :git => 'https://github.com/TextureGroup/Texture.git', :branch => 'master'
end

target :texture do
    shared_pods
end
